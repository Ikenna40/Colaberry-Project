# execution

Legacy Python execution scripts (pre-Node migration). Read-only reference once populated — new work goes in `/backend` or `/scripts`, never here.

Empty for now — there is no pre-Node system in this repo to migrate from. This folder exists per the root CLAUDE.md's Folder Responsibilities but has nothing to hold yet.
