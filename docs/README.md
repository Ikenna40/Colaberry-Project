# docs

In-repo documentation that ships with the codebase (architecture notes, integration guides, system docs). Not the same as root `ARCHITECTURE.md`/`PROGRESS.md`, which are governance artifacts, not product docs.

Empty for now.
