# frontend

React + CRA + TypeScript UI. See [CLAUDE.md](./CLAUDE.md) in this folder for AI-agent conventions.

Structure:
- `src/pages/` — routable pages
- `src/components/` — reusable UI
- `src/routes/` — route definitions

Not yet scaffolded: `package.json`, `tsconfig.json`, and any dependency — pending explicit approval, since adding them means choosing and installing real packages.
