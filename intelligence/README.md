# intelligence

Reserved/in-flight intelligence subsystem outside the main backend tree. Root CLAUDE.md requires checking here vs `backend/src/intelligence/` before adding anything, to avoid duplicating ownership. Both folders currently exist and are both empty — resolve which owns what before either gets real files.

Empty for now.
